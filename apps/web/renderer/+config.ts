import vikeReact from 'vike-react/config';

// eslint-disable-next-line @typescript-eslint/no-explicit-any
const config: any = {
  extends: vikeReact,
  title: 'secretdef — Declare your secret dependencies',
};

export default config;
